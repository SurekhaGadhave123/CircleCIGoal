version: 2.1
orbs:
  cypress: cypress-io/cypress@3
workflows:
  use-my-orb:
    jobs:
      cypress-run:
          start-command: 'npm install'
          cypress-command: 'npx cypress run'

    workflows:
      version: 2
      test:
        jobs:
          - cypress-run